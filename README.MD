```
1. Clone repo to local machine
2. run `npm i`
3. make sure environment variable NODE_ENV is set to development
4. from cmd line: run `npm run start-dev`
5. navigate to localhost: http://localhost:8080/ to see the app
```
